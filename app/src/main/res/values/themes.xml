<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyDaktari" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:actionBarWidgetTheme">@style/MyActionBarWidgetTheme</item>

        <item name="android:autoCompleteTextViewStyle">@style/CustomAutoCompleteTextView</item>
    </style>
    <style name="CustomAutoCompleteTextView" parent="Widget.AppCompat.AutoCompleteTextView">
        <item name="android:dropDownListViewStyle">@style/CustomDropDownListView</item>
    </style>

    <style name="CustomDropDownListView" parent="Widget.AppCompat.ListView.DropDown">
        <item name="android:divider">@null</item>
        <item name="android:dividerHeight">0dp</item>
        <item name="android:listSelector">@android:color/transparent</item>
    </style>
    <style name="MyActionBarWidgetTheme" parent="@android:style/Theme.Holo">
        .....
        .....
        <item name="android:textColorHint">@color/black</item>
    </style>

    <style name="Theme.MyDaktari" parent="Base.Theme.MyDaktari" />
</resources>